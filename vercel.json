{
  "buildCommand": "cd MughalSteelFrontEndApplication && npm install --ignore-scripts=false && npm run build",
  "outputDirectory": "MughalSteelFrontEndApplication/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
